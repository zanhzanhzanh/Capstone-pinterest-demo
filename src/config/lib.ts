// Import librarys
import express, { Request, Response, NextFunction } from 'express';

// Use Module
const router = express.Router();
const app = express();

export { app, router, Request, Response, NextFunction }